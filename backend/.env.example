# ============= 环境变量配置示例 =============
# 复制此文件为 .env 并填入实际值

# ============= 基础配置 =============
# 运行环境: development | production
NODE_ENV=production

# 服务器端口（Render 会自动设置，本地开发可以改）
PORT=3000

# ============= 数据库配置 =============
# MongoDB Atlas 连接字符串
# 格式: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
# 
# 获取方法:
# 1. 登录 https://cloud.mongodb.com
# 2. 选择你的集群 → Connect → Connect your application
# 3. 复制连接字符串并替换 <username> 和 <password>
MONGODB_URI=mongodb+srv://webreal_user:YOUR_PASSWORD_HERE@cluster0.xxxxx.mongodb.net/webreal?retryWrites=true&w=majority

# ============= 安全配置 =============
# JWT 密钥（用于生成和验证用户令牌）
# 生产环境必须使用强随机字符串！
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-32-characters

# ============= CORS 配置 =============
# 允许访问的前端 URL（用于 CORS）
# 开发环境: http://localhost:8080
# 生产环境: https://your-project.vercel.app
FRONTEND_URL=https://your-project.vercel.app

# ============= 可选配置 =============
# 数据库名称（如果在 MONGODB_URI 中已指定，则不需要）
# DB_NAME=webreal

# 日志级别
# LOG_LEVEL=info

# ============= 部署平台配置说明 =============

# === Render 部署 ===
# 在 Render Dashboard 的 Environment 部分添加以上所有变量
# JWT_SECRET 可以使用 Render 的 "Generate Value" 功能自动生成
# MONGODB_URI 必须手动从 MongoDB Atlas 获取
# FRONTEND_URL 在前端部署完成后填入 Vercel 的 URL

# === 本地开发 ===
# 1. 复制此文件为 .env
# 2. 填入实际的 MongoDB URI 和其他配置
# 3. 确保 .env 文件已添加到 .gitignore（不要提交到 Git）

# ============= 安全提醒 =============
# ⚠️ 永远不要将包含实际密码的 .env 文件提交到 Git！
# ⚠️ 生产环境必须使用强密码和随机 JWT_SECRET！
# ⚠️ 定期更换密码和密钥！
